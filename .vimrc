## vim configuration file

## Remap <ESC> to <jk> for leaving INSERT MODE 
inoremap jk <ESC>

## Remap LEADER KEY to <Space>
let mapleader = " "

filetype plugin indent on
syntax on
set encoding=utf-8
set clipboard=unnamedplus

